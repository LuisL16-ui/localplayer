package com.cvc953.localplayer.widget

import org.junit.Test
import org.junit.Assert.assertNotNull

class PlayerWidgetTest {

    @Test
    fun `PlayerWidget can be instantiated`() {
        val widget = PlayerWidget()
        assertNotNull(widget)
    }
}